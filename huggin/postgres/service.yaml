apiVersion: v1
kind: Service
metadata:
  name: huggin-postgres
spec:
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
      targetPort: postgres
  selector:
    name: huggin-postgres
